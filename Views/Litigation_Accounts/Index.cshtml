@model IEnumerable<ActivityUpdate.Models.Litigation_Accounts>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LitigationCaseID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccountNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LitigationCaseID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccountNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LitigationAccountID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LitigationAccountID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LitigationAccountID })
        </td>
    </tr>
}

</table>
